# Track Leader Environment Configuration

# Database connection
DATABASE_URL=postgres://docker:pg@localhost:5432/tracks

# Object store path for GPX files
OBJECT_STORE_PATH=./uploads

# Backend API port (Next.js proxies to this)
PORT=3001

# Logging level
RUST_LOG=tracks=info

# PASETO authentication key (v4.local symmetric encryption)
# For production: use a 64-character hex string (32 bytes)
# Example: openssl rand -hex 32
# For development: any string works (padded/truncated to 32 bytes)
# PASETO_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
